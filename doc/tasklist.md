# План разработки Tuda-Suda App

---

## 📊 Прогресс

| Итерация | Название | Статус | Дата начала | Дата завершения |
|:--------:|----------|:------:|:-----------:|:---------------:|
| 1 | Скелет приложения | ✅ Готово | 23.12.2024 | 23.12.2024 |
| 2 | Конфигурация | ✅ Готово | 23.12.2024 | 23.12.2024 |
| 3 | VPN-мониторинг | ✅ Готово | 23.12.2024 | 23.12.2024 |
| 4 | Мониторинг стендов | ✅ Готово | 23.12.2024 | 23.12.2024 |
| 5 | Получение токена | ✅ Готово | 23.12.2024 | 23.12.2024 |
| 6 | Управление ролями | ✅ Готово | 23.12.2024 | 23.12.2024 |
| 7 | Логирование | ✅ Готово | 23.12.2024 | 23.12.2024 |
| 8 | Сборка и деплой | ✅ Готово | 23.12.2024 | 23.12.2024 |
| 9 | Тестирование | ✅ Готово | 24.12.2024 | 24.12.2024 |
| 10 | Ссылки на Swagger | ✅ Готово | 24.12.2024 | 24.12.2024 |
| 11 | Редизайн (Fluent) | ✅ Готово | 24.12.2024 | 24.12.2024 |
| 12 | SSO-аутентификация | ✅ Готово | 25.12.2024 | 25.12.2024 |
| 13 | Расширенный мониторинг | ✅ Готово | 25.12.2024 | 25.12.2024 |
| 14 | Кроссплатформенная сборка | ✅ Готово | 25.12.2024 | 25.12.2024 |

**Легенда:** ⬜ Ожидает · 🔄 В работе · ✅ Готово · ❌ Отменено

---

## Итерации

### Итерация 1: Скелет приложения

**Цель:** Запускаемое окно с навигацией

- [x] Создать структуру проекта и `pyproject.toml`
- [x] Реализовать `main.py` — точка входа
- [x] Создать `Main.qml` — главное окно с боковой навигацией
- [x] Добавить 4 пустые страницы: Стенды, Токен, Роли, Логи
- [x] Добавить заглушки VPN-индикаторов в шапку

**Тест:** Приложение запускается, навигация переключает страницы ✅

---

### Итерация 2: Конфигурация

**Цель:** Загрузка настроек из YAML

- [x] Создать `config.yaml` с примером стенда и VPN
- [x] Реализовать `config.py` — парсинг конфига с pydantic
- [x] Показать ошибку при отсутствии конфига
- [x] Вывести список стендов из конфига на странице "Стенды"

**Тест:** Стенды из конфига отображаются в UI ✅

---

### Итерация 3: VPN-мониторинг

**Цель:** Индикаторы VPN в реальном времени

- [x] Создать `services/vpn_checker.py` — проверка доступности URL
- [x] Реализовать проверку VPN в фоновом потоке (QThread)
- [x] Обновлять индикаторы каждые 30 сек
- [x] Цветовая индикация: 🟢 доступен / 🔴 недоступен

**Тест:** Индикаторы реагируют на состояние VPN ✅

---

### Итерация 4: Мониторинг стендов

**Цель:** Статус и версия стендов

- [x] Создать `services/stand_checker.py` — REST-запросы к стендам
- [x] Добавить кнопку "Обновить" на странице "Стенды"
- [x] Отображать статус (online/offline) и версию
- [x] Цветовая индикация статуса в списке

**Тест:** Кнопка "Обновить" показывает актуальный статус стендов ✅

---

### Итерация 5: Получение токена

**Цель:** Получение и копирование токена

- [x] Создать `services/token_service.py` — запрос токена
- [x] Добавить выпадающий список стендов на странице "Токен"
- [x] Кнопка "Получить токен" → вывод результата
- [x] Автоматическое копирование токена в буфер обмена

**Тест:** Токен получен и скопирован в буфер ✅

---

### Итерация 6: Управление ролями

**Цель:** Смена роли пользователя в БД стенда

- [x] Создать `services/role_service.py` — работа с PostgreSQL
- [x] Добавить выбор стенда и поле email на странице "Роли"
- [x] Кнопки "Сделать админом" / "Сделать юзером"
- [x] Показать результат операции

**Тест:** Роль пользователя изменена в БД стенда ✅

---

### Итерация 7: Логирование

**Цель:** Централизованный лог всех операций

- [x] Создать `services/app_logger.py` — внутренний логгер
- [x] Реализовать страницу "Логи" с TextArea
- [x] Логировать все операции из предыдущих итераций
- [x] Формат: `[timestamp] LEVEL: сообщение`

**Тест:** Все действия отображаются в логах в реальном времени ✅

---

### Итерация 8: Сборка и деплой

**Цель:** Готовый исполняемый файл

- [x] Обновить `pyproject.toml` со всеми зависимостями
- [x] Настроить PyInstaller (`tuda-suda.spec`)
- [x] Собрать под Linux
- [x] Обновить README

**Тест:** Приложение собрано в `dist/tuda-suda` ✅

---

### Итерация 9: Тестирование

**Цель:** Покрытие кода unit и интеграционными тестами

- [x] Настроить pytest и структуру `tests/`
- [x] Unit-тесты для `config.py` — валидация конфига, обработка ошибок
- [x] Unit-тесты для `services/vpn_checker.py` — mock requests, проверка статусов
- [x] Unit-тесты для `services/stand_checker.py` — mock API, парсинг ответов
- [x] Unit-тесты для `services/token_service.py` — mock API, обработка ошибок
- [x] Unit-тесты для `services/role_service.py` — mock psycopg, SQL-запросы
- [x] Unit-тесты для `services/app_logger.py` — форматирование, сигналы
- [x] Интеграционные тесты контроллеров с mock-сервисами
- [x] Добавить pytest в dev-зависимости и команду запуска в README

**Тест:** `poetry run pytest` — 62 теста проходят ✅

---

### Итерация 10: Ссылки на Swagger

**Цель:** Быстрый доступ к документации API стендов

- [x] Добавить в `config.yaml` поля `core_swagger_url` и `external_swagger_url` для каждого стенда
- [x] Обновить pydantic-модель `Stand` в `config.py`
- [x] Обновить `StandsController` — передавать swagger URL в QML
- [x] Добавить в `StandsView.qml` кнопки/ссылки "Core Swagger" и "External Swagger"
- [x] Реализовать открытие URL в системном браузере (`Qt.openUrlExternally`)
- [x] Обработать случай отсутствия URL (скрыть кнопку или disabled)

**Тест:** Клик по ссылке открывает Swagger в браузере ✅

---

### Итерация 11: Редизайн (Fluent Design)

**Цель:** Современный интерфейс в стиле Fluent Design (тёмная тема)

- [x] Создать `qml/Theme.qml` — цветовая палитра Fluent Dark
  - Акцентный цвет (синий #0078D4)
  - Фоны: #1A1A1A, #202020, #2D2D2D, #383838
  - Текст: #FFFFFF, #A0A0A0
  - Состояния: hover, pressed, disabled
- [x] Обновить навигационную панель — Fluent NavigationView стиль
  - Подсветка активного пункта
  - Hover-эффекты
  - Иконки для пунктов меню
- [x] Обновить шапку — минималистичный стиль с VPN-индикаторами
- [x] Обновить карточки стендов — скруглённые углы, hover с подъёмом
- [x] Обновить кнопки — Fluent Button стиль с анимацией
- [x] Обновить поля ввода — Fluent TextBox стиль
- [x] Обновить ComboBox — стилизованный с hover
- [x] Обновить ScrollView и TextArea на странице логов
- [x] Добавить micro-interactions (hover, click feedback, пульсация)

**Тест:** Интерфейс выглядит современно, все элементы в едином стиле Fluent Dark ✅

---

### Итерация 12: SSO-аутентификация

**Цель:** Реализация двухшаговой аутентификации через Keycloak SSO

- [x] Обновить `token_service.py` — двухшаговое получение токена
  - Шаг 1: GET к `/backend/auth` для получения SSO location из header
  - Шаг 2: POST к `/auth/realms/jaga/protocol/openid-connect/token` с credentials
- [x] Добавить поля логина и пароля в `TokenView.qml`
  - Поле логина с автофокусом и выделением
  - Поле пароля с маскировкой ввода (`echoMode: Password`)
  - Стилизация в Fluent Design с hover-эффектами
- [x] Обновить `token_controller.py` — передача username/password в сервис
  - Валидация наличия логина и пароля перед запросом
  - Обновить сигнатуру `@Slot(str, str, str)` для 3 параметров
- [x] Обновить unit-тесты для нового алгоритма
  - Mock двухшагового процесса (GET + POST)
  - Тесты на отсутствие Location header
  - Тесты на ошибки SSO (401, пустой access_token)

**Тест:** Токен успешно получается через SSO с логином/паролем ✅

---

### Итерация 13: Расширенный мониторинг стендов

**Цель:** Отображение тега сборки и ветки для каждого стенда

- [x] Добавить `health_check_endpoint` в модель `Stand` (`config.py`)
  - Значение по умолчанию: `/api/health`
- [x] Обновить `config.yaml` — добавить примеры `health_check_endpoint` для стендов
- [x] Обновить `stand_checker.py` — читать headers `x-tag` и `x-branch-name`
  - Изменить сигнал `stand_checked` для передачи tag и branch
  - Обновить метод `_check_stand()` для возврата 4 значений
  - Добавить параметр `verify=False` для игнорирования SSL
- [x] Обновить `stands_controller.py` — хранить и передавать tag и branch
  - Добавить поля `tag` и `branch` в `_stands_data`
  - Обновить обработчик `_on_stand_checked()` для 5 параметров
- [x] Обновить `StandsView.qml` — отобразить tag и branch
  - Секция с тегом (🏷️) и веткой (🌿) под URL
  - Показывать только для online стендов
- [x] Обновить unit-тесты для `stand_checker.py`
  - Mock headers в ответах
  - Проверка всех 4 возвращаемых значений
- [x] Обновить интеграционные тесты для `StandsController`

**Тест:** Карточка стенда отображает тег и ветку при статусе online ✅

---

### Итерация 14: Кроссплатформенная сборка (Docker)

**Цель:** Решение проблемы совместимости GLIBC — сборка для максимальной совместимости с различными Linux-системами

**Проблема:** Дистрибутив собран на системе с GLIBC 2.38, не запускается на системах со старыми GLIBC (2.31-2.35)

- [x] Создать `Dockerfile` на базе Ubuntu 20.04 (GLIBC 2.31)
  - Установка Python 3.12, Poetry, зависимостей Qt
  - Копирование проекта и сборка PyInstaller внутри контейнера
- [x] Создать `build.sh` скрипт для автоматической Docker-сборки
  - Сборка образа
  - Запуск контейнера и копирование артефактов в `dist-docker/`
- [x] Обновить `.gitignore` — добавить `dist-docker/`
- [x] Обновить `BUILD_REPORT.md` — добавить секцию о кроссплатформенной сборке
- [x] Создать `.dockerignore` для оптимизации сборки
- [x] Обновить `README.md` — добавить инструкции по Docker-сборке и установке Docker
- [x] Обновить `tasklist.md` — добавить итерацию 14
- [x] Установить Docker на целевой системе
- [x] Исправить Dockerfile — использовать Ubuntu 22.04 (GLIBC 2.35) вместо 20.04
- [x] Исправить установку pip для Python 3.12
- [x] Успешно собрать приложение в Docker
- [x] Проверить зависимости бинарника
- [x] Создать директорию `distribution/` для файлов сборки
- [x] Переместить Dockerfile и скрипты в `distribution/`
- [x] Создать `build_in_docker.sh` (переименован из build.sh)
- [x] Создать `build_native.sh` для сборки без Docker
- [x] Исправить Dockerfile — вернуться к Ubuntu 22.04 (GLIBC 2.35) из-за проблем совместимости PySide6
- [x] Обновить все пути в скриптах для работы из `distribution/`
- [x] Создать `distribution/README.md` с инструкциями
- [x] Обновить документацию проекта
- [x] Удалить старые файлы из корня проекта (Dockerfile, build.sh, .dockerignore, DOCKER_BUILD_GUIDE.md)
- [x] Обновить .gitignore для distribution/

**Результат:**
- Все файлы сборки в `distribution/`
- Dockerfile: Ubuntu 22.04 + Python 3.12 (GLIBC 2.35)
- Два скрипта: `build_in_docker.sh` (Docker) и `build_native.sh` (нативная сборка)
- Для систем с GLIBC < 2.35 рекомендуется нативная сборка на целевой системе

**Решение проблемы GLIBC:**
- Docker-сборка: GLIBC 2.35 (Ubuntu 22.04+, Debian 12+, RHEL 9+)
- Нативная сборка: для старых систем - клонировать репозиторий и собрать на месте

**Готово к коммиту:** ✅
