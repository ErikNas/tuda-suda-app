# Сборка Tuda-Suda App

Эта директория содержит инструменты для сборки приложения.

## 🐳 Docker-сборка (рекомендуется)

**Преимущества:**
- Хорошая совместимость (GLIBC 2.35, Ubuntu 22.04)
- Изолированная среда сборки
- Не требует установки Python 3.12 на хост-системе

**Примечание:** Если целевая система имеет GLIBC < 2.35, используйте нативную сборку на целевой системе.

**Требования:**
- Docker

**Команда:**
```bash
cd distribution
./build_in_docker.sh
```

**Результат:** `distribution/dist-docker/`

---

## 💻 Нативная сборка

**Преимущества:**
- Быстрее (нет оверхеда Docker)
- Бинарник оптимизирован для текущей системы

**Требования:**
- Python 3.12
- Poetry

**Команда:**
```bash
cd distribution
./build_native.sh
```

**Результат:** `distribution/dist-native/`

---

## 🎯 Целевая система

### Docker-сборка
Совместимость с системами:
- ✅ Ubuntu 22.04+ (GLIBC 2.35+)
- ✅ Debian 12+
- ✅ RHEL 9+, Rocky Linux 9+
- ✅ Fedora 35+
- ✅ ALT Linux с GLIBC 2.35+

### Нативная сборка (рекомендуется для старых систем)
Совместимость зависит от версии GLIBC на системе сборки.
Собранный бинарник будет работать на системах с GLIBC >= версии системы сборки.

**Для систем с GLIBC < 2.35:** Клонируйте репозиторий на целевой системе и запустите `./build_native.sh`

---

## 📂 Структура

```
distribution/
├── Dockerfile              # Docker-образ для сборки
├── build_in_docker.sh      # Скрипт Docker-сборки
├── build_native.sh         # Скрипт нативной сборки
├── dist-docker/            # Результат Docker-сборки
├── dist-native/            # Результат нативной сборки
└── README.md               # Эта инструкция
```

---

## 🔧 Установка зависимостей

### Docker (ALT Linux)
```bash
sudo apt-get update
sudo apt-get install docker-ce
sudo usermod -aG docker $USER
sudo systemctl start docker
sudo systemctl enable docker
```

### Python 3.12 и Poetry (для нативной сборки)
```bash
# ALT Linux
sudo apt-get install python3.12 python3.12-dev python3.12-venv

# Poetry
curl -sSL https://install.python-poetry.org | python3 -
```

---

**Последнее обновление:** 25.12.2024

